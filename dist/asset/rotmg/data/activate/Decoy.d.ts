import { Activate } from "./Activate";
export declare class Decoy implements Activate {
    duration: number;
    speed: number;
    distance: number;
    angleOffset: number;
    getName(): string;
}
